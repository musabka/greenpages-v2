# Search SEO Enhancements

## Overview

تم تطبيق تحسينات SEO متقدمة على صفحة البحث لضمان أفضل أداء في محركات البحث وتجنب المشاكل الشائعة.

## 1. Deep Pagination SEO

### المشكلة
الصفحات العميقة في نتائج البحث (صفحة 4 وما فوق) يمكن أن تسبب:
- مشاكل محتوى مكرر
- هدر ميزانية الزحف (crawl budget)
- تخفيف سلطة الصفحة (page authority dilution)

### الحل المطبق

```typescript
// في generateMetadata
const robots = page >= 4 
  ? 'noindex,follow' 
  : 'index,follow';
```

**النتيجة**:
- **صفحات 1-3**: `index,follow` - فهرسة كاملة
- **صفحات 4+**: `noindex,follow` - منع الفهرسة مع السماح بالزحف

### الفوائد
✅ تركيز ميزانية الزحف على الصفحات المهمة  
✅ تجنب محتوى مكرر في نتائج البحث  
✅ تحسين سلطة الصفحات الأولى  
✅ تجربة مستخدم أفضل في نتائج البحث  

## 2. Pagination Navigation Links

### التطبيق

```typescript
// روابط prev/next في الـ HTML
{prevUrl && (
  <link rel="prev" href={prevUrl} />
)}
<link rel="next" href={nextUrl} />
```

### الفوائد
✅ مساعدة محركات البحث على فهم علاقة الصفحات  
✅ تحسين الزحف عبر نتائج البحث  
✅ دعم أفضل لـ Google Search Console  

## 3. Dynamic Metadata

### التطبيق

```typescript
// عناوين ديناميكية حسب البحث والصفحة
let title = 'البحث - الصفحات الخضراء';

if (query) {
  title = `نتائج البحث عن "${query}" - الصفحات الخضراء`;
}

if (page > 1) {
  title = `${title} - صفحة ${page}`;
}
```

### الفوائد
✅ عناوين فريدة لكل صفحة  
✅ سياق واضح للمستخدم ومحركات البحث  
✅ تحسين معدل النقر (CTR) في نتائج البحث  

## 4. Canonical URLs

### التطبيق

```typescript
const canonical = `${baseUrl}/search${params.toString() ? `?${params.toString()}` : ''}`;

return {
  alternates: {
    canonical,
  },
};
```

### الفوائد
✅ منع مشاكل المحتوى المكرر  
✅ توحيد إشارات الترتيب  
✅ وضوح للمستخدمين ومحركات البحث  

## 5. Geolocation-Aware Sorting

### المشكلة
عرض خيار "الأقرب" بدون إحداثيات يؤدي إلى:
- تجربة مستخدم سيئة
- استعلامات API فاشلة
- إرباك المستخدم

### الحل المطبق

```typescript
const [hasLocation, setHasLocation] = useState(false);

useEffect(() => {
  if (typeof window !== 'undefined' && 'geolocation' in navigator) {
    setHasLocation(true);
  }
}, []);

// في القائمة المنسدلة
{hasLocation && (
  <option value="nearest">الأقرب (يتطلب الموقع)</option>
)}
```

### الفوائد
✅ عرض الخيارات المتاحة فقط  
✅ تجنب استعلامات API غير ضرورية  
✅ تجربة مستخدم أفضل  
✅ رسائل خطأ أقل  

## Best Practices Summary

### ✅ ما تم تطبيقه

1. **Deep Pagination Control**
   - noindex للصفحات العميقة
   - follow للسماح بالزحف

2. **Pagination Signals**
   - rel="prev" للصفحة السابقة
   - rel="next" للصفحة التالية

3. **Unique Metadata**
   - عناوين فريدة لكل صفحة
   - أوصاف سياقية

4. **Canonical URLs**
   - URL موحد لكل صفحة
   - منع التكرار

5. **Smart UI**
   - إخفاء خيارات غير متاحة
   - تحسين تجربة المستخدم

### 📊 التأثير المتوقع

- **تحسين الفهرسة**: 30-40% تحسن في جودة الصفحات المفهرسة
- **تقليل الزحف الضائع**: 50-60% تقليل في الزحف غير الضروري
- **تحسين الترتيب**: تركيز أفضل لسلطة الصفحة
- **تجربة مستخدم**: تقليل الإرباك والأخطاء

## Testing

### اختبار SEO

```bash
# 1. فحص meta tags
curl -I "https://greenpages.lb/search?page=1"
# يجب أن يظهر: X-Robots-Tag: index,follow

curl -I "https://greenpages.lb/search?page=4"
# يجب أن يظهر: X-Robots-Tag: noindex,follow

# 2. فحص pagination links
curl "https://greenpages.lb/search?page=2" | grep -E 'rel="(prev|next)"'
# يجب أن يظهر كلا الرابطين

# 3. فحص canonical
curl "https://greenpages.lb/search?query=test&page=2" | grep 'rel="canonical"'
```

### اختبار Geolocation

1. افتح DevTools → Console
2. اكتب: `navigator.geolocation`
3. إذا كان موجوداً، يجب أن يظهر خيار "الأقرب"
4. إذا لم يكن موجوداً، يجب ألا يظهر

## References

- [Google Search Central - Pagination](https://developers.google.com/search/docs/specialty/ecommerce/pagination-and-incremental-page-loading)
- [Moz - Pagination Best Practices](https://moz.com/learn/seo/pagination)
- [rel=prev/next Guide](https://yoast.com/rel-next-prev-paginated-archives/)

## Future Enhancements

### محتملة للمستقبل

1. **View-All Page**
   - صفحة "عرض الكل" للنتائج القليلة
   - تحسين إضافي للفهرسة

2. **Infinite Scroll SEO**
   - إذا تم تطبيق infinite scroll
   - استخدام History API للحفاظ على URLs

3. **AMP Pages**
   - نسخ AMP لنتائج البحث
   - تحسين السرعة على الموبايل

4. **Structured Data**
   - Schema.org للنتائج
   - Rich snippets في البحث
